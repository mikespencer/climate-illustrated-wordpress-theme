# Climate Illustrated Custom Theme
Child of the _magazine_ theme (required)

### This theme is in development.

## Development

Clone this repository into a folder named `climate-illustrated` in your wordpress' theme folder.

Make sure you have Node.js and Gulp.js installed.

Open up a terminal, `cd` into this `climate-illustrated` directory and run `npm install`.

Run `gulp`.
